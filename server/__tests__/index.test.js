import Controller from '../index.js'
//I think the way jest checks, this file is suppsoed to test db index file as well

describe('Server Controller Component', () => {
  it ('this is a placholder', () => {
    expect(0).toEqual(0);
  });
});